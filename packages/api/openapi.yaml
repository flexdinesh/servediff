openapi: 3.1.0
info:
  title: servediff API
  version: 1.0.0
  description: Local diff review API.
servers:
  - url: /
paths:
  /api/v1/metrics:
    get:
      operationId: getServerMetrics
      responses:
        "200":
          description: CPU and resident memory used by the ServeDiff server process.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServerMetrics"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/session:
    get:
      operationId: getSession
      responses:
        "200":
          description: Active diff source and capabilities.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/diffs/current:
    get:
      operationId: getCurrentDiff
      parameters:
        - $ref: "#/components/parameters/Scope"
      responses:
        "200":
          description: Current diff snapshot.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RepositoryDiff"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/diffs/{diffId}/files/{fileId}/patch:
    get:
      operationId: getFilePatch
      parameters:
        - $ref: "#/components/parameters/DiffId"
        - $ref: "#/components/parameters/FileId"
        - $ref: "#/components/parameters/Scope"
        - $ref: "#/components/parameters/FileVersion"
      responses:
        "200":
          description: File patch.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FilePatch"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/diffs/{diffId}/files/{fileId}/contents:
    get:
      operationId: getFileContents
      parameters:
        - $ref: "#/components/parameters/DiffId"
        - $ref: "#/components/parameters/FileId"
        - $ref: "#/components/parameters/Scope"
        - $ref: "#/components/parameters/FileVersion"
      responses:
        "200":
          description: Complete before and after contents.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FileContents"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/comments:
    get:
      operationId: listComments
      responses:
        "200":
          description: All comments for the active source.
          content:
            application/json:
              schema:
                type: object
                required: [comments]
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReviewComment"
        default:
          $ref: "#/components/responses/Problem"
    delete:
      operationId: deleteComments
      parameters:
        - name: status
          in: query
          required: true
          schema:
            type: string
            enum: [all, open, resolved, stale]
      responses:
        "200":
          description: Remaining comments after bulk deletion.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [comments, deleted]
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReviewComment"
                  deleted:
                    type: integer
                    minimum: 0
        default:
          $ref: "#/components/responses/Problem"
    post:
      operationId: createComment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateComment"
      responses:
        "201":
          description: Created comment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewComment"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/comments/import:
    post:
      operationId: importComments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [comments]
              properties:
                comments:
                  type: array
                  items:
                    $ref: "#/components/schemas/ReviewComment"
      responses:
        "200":
          description: Idempotently imported comments.
          content:
            application/json:
              schema:
                type: object
                required: [comments]
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReviewComment"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/comments/export:
    get:
      operationId: exportComments
      parameters:
        - name: includeResolved
          in: query
          required: false
          schema:
            type: boolean
            default: false
        - name: commentId
          in: query
          required: false
          schema:
            type: string
        - name: revision
          in: query
          required: false
          schema:
            type: string
        - name: scope
          in: query
          required: false
          schema:
            $ref: "#/components/schemas/DiffMode"
      responses:
        "200":
          description: Canonical agent-ready export.
          content:
            application/xml:
              schema:
                type: string
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/comments/{commentId}:
    patch:
      operationId: updateComment
      parameters:
        - $ref: "#/components/parameters/CommentId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateComment"
      responses:
        "200":
          description: Updated comment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewComment"
        default:
          $ref: "#/components/responses/Problem"
    delete:
      operationId: deleteComment
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "204":
          description: Comment deleted.
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/review/comments:
    get:
      operationId: listAgentReviewComments
      parameters:
        - name: includeResolved
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "200":
          description: Agent-ready comments with current applicability.
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                required: [comments]
                properties:
                  comments:
                    type: array
                    items:
                      $ref: "#/components/schemas/AgentReviewComment"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/review/comments/{commentId}/resolve:
    post:
      operationId: resolveAgentReviewComment
      parameters:
        - $ref: "#/components/parameters/CommentId"
      responses:
        "200":
          description: Idempotently resolved comment.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResolveReviewComment"
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/review-marks:
    get:
      operationId: listReviewMarks
      parameters:
        - $ref: "#/components/parameters/Scope"
      responses:
        "200":
          description: Reviewed file versions.
          content:
            application/json:
              schema:
                type: object
                required: [marks]
                properties:
                  marks:
                    type: array
                    items:
                      $ref: "#/components/schemas/ReviewMark"
        default:
          $ref: "#/components/responses/Problem"
    delete:
      operationId: clearReviewMarks
      parameters:
        - $ref: "#/components/parameters/Scope"
      responses:
        "204":
          description: Scope review marks cleared.
        default:
          $ref: "#/components/responses/Problem"
  /api/v1/review-marks/{fileId}:
    put:
      operationId: putReviewMark
      parameters:
        - $ref: "#/components/parameters/FileId"
        - $ref: "#/components/parameters/Scope"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [fileVersion]
              properties:
                fileVersion:
                  type: string
      responses:
        "200":
          description: Stored review mark.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ReviewMark"
        default:
          $ref: "#/components/responses/Problem"
    delete:
      operationId: deleteReviewMark
      parameters:
        - $ref: "#/components/parameters/FileId"
        - $ref: "#/components/parameters/Scope"
      responses:
        "204":
          description: Review mark deleted.
        default:
          $ref: "#/components/responses/Problem"
components:
  parameters:
    Scope:
      name: scope
      in: query
      required: true
      schema:
        $ref: "#/components/schemas/DiffMode"
    DiffId:
      name: diffId
      in: path
      required: true
      schema:
        type: string
    FileId:
      name: fileId
      in: path
      required: true
      schema:
        type: string
    FileVersion:
      name: fileVersion
      in: query
      required: true
      schema:
        type: string
    CommentId:
      name: commentId
      in: path
      required: true
      schema:
        type: string
  responses:
    Problem:
      description: Request failed.
      content:
        application/problem+json:
          schema:
            $ref: "#/components/schemas/Problem"
  schemas:
    ServerMetrics:
      type: object
      additionalProperties: false
      required: [rssBytes, cpuUsage]
      properties:
        rssBytes:
          type: number
          minimum: 0
        cpuUsage:
          type: number
          minimum: 0
          maximum: 100
    DiffMode:
      type: string
      enum: [all, staged, unstaged]
    CapabilityState:
      type: string
      enum: [enabled, disabled, unavailable]
    Capability:
      type: object
      additionalProperties: false
      required: [state]
      properties:
        state:
          $ref: "#/components/schemas/CapabilityState"
    ScopesCapability:
      type: object
      additionalProperties: false
      required: [state, values]
      properties:
        state:
          $ref: "#/components/schemas/CapabilityState"
        values:
          type: array
          items:
            $ref: "#/components/schemas/DiffMode"
    Session:
      type: object
      additionalProperties: false
      required: [id, source, name, root, capabilities]
      properties:
        id:
          type: string
        source:
          type: string
          enum: [local, stdin]
        name:
          type: string
        root:
          type: string
        capabilities:
          type: object
          additionalProperties: false
          required: [diff, files, review]
          properties:
            diff:
              type: object
              additionalProperties: false
              required: [scopes, refresh, stagingMetadata]
              properties:
                scopes:
                  $ref: "#/components/schemas/ScopesCapability"
                refresh:
                  $ref: "#/components/schemas/Capability"
                stagingMetadata:
                  $ref: "#/components/schemas/Capability"
            files:
              type: object
              additionalProperties: false
              required: [contents]
              properties:
                contents:
                  $ref: "#/components/schemas/Capability"
            review:
              type: object
              additionalProperties: false
              required: [comments]
              properties:
                comments:
                  $ref: "#/components/schemas/Capability"
    ChangedFile:
      type: object
      additionalProperties: false
      required:
        [
          id,
          path,
          oldPath,
          status,
          indexStatus,
          worktreeStatus,
          additions,
          deletions,
          binary,
          fingerprint,
        ]
      properties:
        id:
          type: string
        path:
          type: string
        oldPath:
          type: [string, "null"]
        status:
          type: string
        indexStatus:
          type: string
        worktreeStatus:
          type: string
        additions:
          type: integer
        deletions:
          type: integer
        binary:
          type: boolean
        fingerprint:
          type: string
        recreated:
          type: boolean
    RepositoryDiff:
      type: object
      additionalProperties: false
      required: [source, root, name, branch, head, mode, files, revision]
      properties:
        source:
          type: string
          enum: [local, stdin]
        root:
          type: string
        name:
          type: string
        branch:
          type: string
        head:
          type: [string, "null"]
        mode:
          $ref: "#/components/schemas/DiffMode"
        files:
          type: array
          items:
            $ref: "#/components/schemas/ChangedFile"
        revision:
          type: string
    FilePatch:
      type: object
      additionalProperties: false
      required: [patch, message]
      properties:
        patch:
          type: string
        message:
          type: [string, "null"]
        contents:
          $ref: "#/components/schemas/FileContents"
    FileContents:
      type: object
      additionalProperties: false
      required: [before, after]
      properties:
        before:
          type: string
        after:
          type: string
    ReviewOrigin:
      type: object
      additionalProperties: false
      required: [source, repository, branch, head, revision, file]
      properties:
        source:
          type: string
          enum: [local, stdin]
        repository:
          type: string
        branch:
          type: string
        head:
          type: [string, "null"]
        revision:
          type: string
        file:
          type: object
          additionalProperties: false
          required: [status, oldPath]
          properties:
            status:
              type: string
            oldPath:
              type: [string, "null"]
    ReviewComment:
      type: object
      additionalProperties: false
      required:
        [
          id,
          path,
          scope,
          fingerprint,
          side,
          start,
          end,
          code,
          body,
          status,
          createdAt,
        ]
      properties:
        id:
          type: string
        path:
          type: string
        scope:
          $ref: "#/components/schemas/DiffMode"
        fingerprint:
          type: string
        side:
          type: string
          enum: [additions, deletions]
        start:
          type: integer
          minimum: 1
        end:
          type: integer
          minimum: 1
        code:
          type: string
        body:
          type: string
        status:
          type: string
          enum: [open, resolved]
        createdAt:
          type: number
        origin:
          $ref: "#/components/schemas/ReviewOrigin"
    AgentReviewComment:
      type: object
      additionalProperties: false
      required:
        [
          id,
          path,
          scope,
          fingerprint,
          side,
          start,
          end,
          code,
          body,
          status,
          createdAt,
          applicability,
          actionable,
        ]
      properties:
        id:
          type: string
        path:
          type: string
        scope:
          $ref: "#/components/schemas/DiffMode"
        fingerprint:
          type: string
        side:
          type: string
          enum: [additions, deletions]
        start:
          type: integer
          minimum: 1
        end:
          type: integer
          minimum: 1
        code:
          type: string
        body:
          type: string
        status:
          type: string
          enum: [open, resolved]
        createdAt:
          type: number
        origin:
          $ref: "#/components/schemas/ReviewOrigin"
        applicability:
          type: string
          enum: [anchored, stale, other-scope, unknown]
        actionable:
          type: boolean
    ResolveReviewComment:
      type: object
      additionalProperties: false
      required: [comment_id, status]
      properties:
        comment_id:
          type: string
        status:
          type: string
          enum: [resolved]
    CreateComment:
      type: object
      additionalProperties: false
      required: [diffId, fileId, scope, fileVersion, side, start, end, body]
      properties:
        diffId:
          type: string
        fileId:
          type: string
        scope:
          $ref: "#/components/schemas/DiffMode"
        fileVersion:
          type: string
        side:
          type: string
          enum: [additions, deletions]
        start:
          type: integer
          minimum: 1
        end:
          type: integer
          minimum: 1
        body:
          type: string
    UpdateComment:
      type: object
      additionalProperties: false
      minProperties: 1
      properties:
        body:
          type: string
        status:
          type: string
          enum: [open, resolved]
    ReviewMark:
      type: object
      additionalProperties: false
      required: [fileId, fileVersion, scope]
      properties:
        fileId:
          type: string
        fileVersion:
          type: string
        scope:
          $ref: "#/components/schemas/DiffMode"
    Problem:
      type: object
      additionalProperties: false
      required: [type, title, status, detail]
      properties:
        type:
          type: string
        title:
          type: string
        status:
          type: integer
        detail:
          type: string
        code:
          type: string
        capability:
          type: string
